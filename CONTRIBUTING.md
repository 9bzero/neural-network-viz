# Contributing

Open an issue before working on anything significant — especially visualization logic, which is easy to get subtly wrong.

Bug fixes can come as PRs directly.

```bash
npm install && npm run dev
```

If you're changing how an algorithm is visualized, describe the visual change in the PR.